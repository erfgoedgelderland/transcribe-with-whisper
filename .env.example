# =========================
# Voorbeeld .env bestand
# Vul deze waarden in of pas ze aan naar jouw voorkeur
# =========================

# ============ Whisper compute type ============
# Bepaalt hoe de modellen worden uitgevoerd.
# Kies afhankelijk van je hardware (CPU of GPU):
# - CPU: int8_float32 (aanbevolen: klein, snel, redelijke nauwkeurigheid)
#        int8 (nog kleiner en sneller, iets lagere nauwkeurigheid)
# - GPU (NVIDIA): float16 (aanbevolen: sneller en zuiniger geheugen)
#                 float32 (volledige precisie, maar zwaar en traag)
FASTER_WHISPER_COMPUTE=int8_float32

# ============ Output strategie ============
# Waar moeten de transcripties opgeslagen worden?
# - next_to_audio (standaard): transcript naast het audiobestand
# - outputs_dir: alle transcripties in een aparte output-map
TRANSCRIBER_OUTPUT_STRATEGY=next_to_audio

# Alleen relevant als OUTPUT_STRATEGY = outputs_dir
# Zet hier een absoluut pad of laat leeg voor de standaard ./outputs map
TRANSCRIBER_OUTPUT_DIR=

# ============ HuggingFace Hub ============
# Voortgangsbalken in console tonen of uitzetten.
# 1 = uit (standaard), 0 = aan
HF_HUB_DISABLE_PROGRESS_BARS=1
